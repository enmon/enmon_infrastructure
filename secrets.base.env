# Place InfluxDB initial username and password here
#INFLUXDB_USERNAME=
#INFLUXDB_PASSWORD=

# InfluxDB admin API token; can be generated with
# `uuidgen` or `gpg --gen-random -a 0 25`
#INFLUXDB_ADMIN_TOKEN=

# OpenWeatherMap API key
#OWM_API_KEY=


# NOTE: quotation marks are interpreted as being part of the
# variable, and not as an escape. This often provokes issues
# as most apps won't handle the value correctly, so it might
# be desirable to avoid using them.
